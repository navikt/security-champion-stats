[versions]
logback = "1.5.31"
logstash = "9.0"
hikari = "7.0.2"
slack = "1.48.0"
postgres = "42.7.10"
jackson-datatype = "3.0.0-rc2"
jackson = "3.1.0"
junit-jupiter = "6.0.3"
spring-doc = "2.8.14"
flyway = "12.2.0"
mockito-kotlin = "6.3.0"

[libraries]

#Spring backend
spring-web = { module = "org.springframework.boot:spring-boot-starter-web" }
spring-boot-data = { module = "org.springframework.boot:spring-boot-starter-data-jpa"}
spring-security = { module = "org.springframework.boot:spring-boot-starter-security"}
spring-oauth = { module = "org.springframework.boot:spring-boot-starter-oauth2-resource-server" }

#Spring frontend
spring-webclient = { module = "org.springframework.boot:spring-boot-starter-webflux"}

#Swagger
swagger = { module = "org.springdoc:springdoc-openapi-starter-webmvc-ui", version.ref = "spring-doc" }

#flyway
flyway-core = { module = "org.flywaydb:flyway-core", version.ref = "flyway" }
flyway-db = { module = "org.flywaydb:flyway-database-postgresql", version.ref = "flyway" }

#Logging
logback-classic = { module = "ch.qos.logback:logback-classic", version.ref = "logback" }
ogstash = { module = "net.logstash.logback:logstash-logback-encoder", version.ref = "logstash" }

#Postgres
hikariCP = { module = "com.zaxxer:HikariCP", version.ref = "hikari" }
postgressql = { module = "org.postgresql:postgresql", version.ref = "postgres" }

#Jackson
jackson-module = { module = "tools.jackson.module:jackson-module-kotlin", version.ref = "jackson" }
jackson-datatype = { module = "tools.jackson.datatype:jackson-datatype-jsr310", version.ref = "jackson-datatype" }
jackson-core = { module = "tools.jackson.core:jackson-databind", version.ref = "jackson" }

#Slack
slack-api = { module = "com.slack.api:slack-api-client", version.ref = "slack" }

#reflect
kotlin-reflect = { module = "org.jetbrains.kotlin:kotlin-reflect", version = "2.2.21" }
kotlin-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version = "1.8.0" }

#jupitor/test
junit-bom = { module = "org.junit:junit-bom", version.ref = "junit-jupiter" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter" }
spring-test = { module = "org.springframework.boot:spring-boot-starter-test" }
mockito-kotlin = { module = "org.mockito.kotlin:mockito-kotlin", version.ref = "mockito-kotlin" }

[bundles]

spring = [
    "spring-boot-data",
    "spring-web",
    "spring-webclient",
    "spring-security",
    "spring-oauth"
]

flyway = [
    "flyway-core",
    "flyway-db"
]

logging = [
    "logback-classic",
    "ogstash"
]

jackson = [
    "jackson-datatype",
    "jackson-module",
    "jackson-core"
]

postgres = [
    "postgressql",
    "hikariCP"
]